# packages.txt — apt system packages for Streamlit Cloud (Ubuntu 22.04)
#
# yfinance >= 0.2.37 uses curl_cffi with precompiled binary wheels,
# so no system-level curl or SSL libraries are required.
#
# This file is intentionally minimal. Add apt package names (one per line)
# only if a future dependency requires compiled system libraries.
#
# Example (not needed now):
# libgomp1
